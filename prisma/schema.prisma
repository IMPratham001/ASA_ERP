generator client {
  provider = "prisma-client-js"
}
```
```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```
```prisma
model Product {
  id          String      @id @default(uuid())
  name        String
  sku         String      @unique
  price       Float
  inventory   Inventory?
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
}
```
```prisma
model Inventory {
  id          String    @id @default(uuid())
  productId   String    @unique
  product     Product   @relation(fields: [productId], references: [id])
  quantity    Int       @default(0)
  minQuantity Int       @default(5)
  location    String
  lastUpdated DateTime  @updatedAt
  createdAt   DateTime  @default(now())

  @@index([quantity])
  @@index([location])
}
```
```prisma
model Customer {
  id        String    @id @default(uuid())
  name      String
  email     String    @unique
  phone     String?
  address   String?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}
```

Now, add the models that are in the original file but missing from the edited snippet: User and Invoice.

```prisma
model User {
  id            String    @id @default(uuid())
  email         String    @unique
  name          String
  role          String    @default("user")
  password      String
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  moduleAccess  Json?
}
```
```prisma
model Invoice {
  id          String    @id @default(uuid())
  customerId  String
  customer    Customer  @relation(fields: [customerId], references: [id])
  number      String    @unique
  date        DateTime
  dueDate     DateTime
  status      String
  items       Json
  total       Decimal
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
}
```

```
</replit_final_file>
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Product {
  id          String      @id @default(uuid())
  name        String
  sku         String      @unique
  price       Float
  inventory   Inventory?
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
}

model Inventory {
  id          String    @id @default(uuid())
  productId   String    @unique
  product     Product   @relation(fields: [productId], references: [id])
  quantity    Int       @default(0)
  minQuantity Int       @default(5)
  location    String
  lastUpdated DateTime  @updatedAt
  createdAt   DateTime  @default(now())

  @@index([quantity])
  @@index([location])
}

model Customer {
  id        String    @id @default(uuid())
  name      String
  email     String    @unique
  phone     String?
  address   String?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}

model User {
  id            String    @id @default(uuid())
  email         String    @unique
  name          String
  role          String    @default("user")
  password      String
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  moduleAccess  Json?
}

model Invoice {
  id          String    @id @default(uuid())
  customerId  String
  customer    Customer  @relation(fields: [customerId], references: [id])
  number      String    @unique
  date        DateTime
  dueDate     DateTime
  status      String
  items       Json
  total       Decimal
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
}
</replit_final_file>